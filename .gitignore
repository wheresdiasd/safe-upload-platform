# Project guide (contains AWS account ID, bucket names, IAM user details)
CLAUDE.md

# AWS
.aws/
*.pem

# Environment variables
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Node (for when we build the frontend/lambda)
node_modules/
dist/
.output/

# Build artifacts
lambda/bin/
lambda/*.zip
lambda/bootstrap
lambda/create-file
lambda/update-file

# Lambda invocation dumps from local debugging — frequently contain
# short-lived presigned URLs and STS temp credentials, never commit.
lambda/output.json

# Test files
testfile.txt
eicar-test.txt
guardduty-test.txt
